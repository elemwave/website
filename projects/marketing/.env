# Hosts the Next.js dev server accepts cross-origin requests from, comma-separated.
# Must match the server_name in docker/nginx/default.conf.
ALLOWED_DEV_ORIGINS=test.localhost.elemwave.com

CALENDLY_URL=https://calendly.com/marina-munoz-aircury
